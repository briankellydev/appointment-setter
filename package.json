{
  "name": "appointment-setter",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server",
    "build": "cd reactapp && npm rebuild node-sass && cd ../ && npm install && cd reactapp && npm install && npm run build && cd ../ && rm -rf public && mkdir public && cpx \"reactapp/build/**/*\" public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^3.0.6",
    "body-parser": "^1.20.1",
    "config": "^3.2.3",
    "express": "^4.18.2",
    "joi": "^14.3.1",
    "jsonwebtoken": "^8.5.1",
    "method-override": "^3.0.0",
    "moment": "^2.24.0",
    "mongoose": "^5.7.3"
  },
  "devDependencies": {
    "cpx": "^1.5.0"
  }
}
